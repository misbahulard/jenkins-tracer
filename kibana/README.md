# Kibana Dashboard and Visualization Example

In this sub directories you can find example dashboard and visualization that
can be imported to your Kibana, for the example we use `jenkins-*` index pattern
name.